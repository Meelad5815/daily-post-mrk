"use client";import {useState} from "react";
export default function Home(){const[topic,setTopic]=useState("Web development tips for Pakistani small businesses");const[post,setPost]=useState<any>(null);const[busy,setBusy]=useState(false);async function generate(){setBusy(true);const r=await fetch("/api/generate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({topic})});setPost((await r.json()).post);setBusy(false)}return <main className="wrap"><header className="top"><div><div className="brand">MRK Daily Post Auto-Pilot</div><div className="muted">AI content • scheduling • multi-platform publishing</div></div><button className="btn" onClick={generate}>{busy?"Generating…":"Generate Today’s Post"}</button></header><section className="grid"><div className="card"><div className="muted">Daily posts</div><div className="num">1</div></div><div className="card"><div className="muted">Platforms</div><div className="num">4</div></div><div className="card"><div className="muted">Approval</div><div className="num">ON</div></div><div className="card"><div className="muted">System</div><div className="num status">READY</div></div></section><section className="main"><div className="card"><h2>Content Generator</h2><p className="muted">Topic / campaign</p><input value={topic} onChange={e=>setTopic(e.target.value)} style={{width:"100%",padding:12,borderRadius:10,border:"1px solid #315070",background:"#081526",color:"#fff"}}/><div className="post">{post?<><span className="tag">{post.title}</span><p style={{whiteSpace:"pre-wrap"}}>{post.body}</p><div>{post.hashtags?.join(" ")}</div></>:<p className="muted">Generate a post to preview it here.</p>}</div></div><aside className="card"><h2>Publishing</h2><p className="muted">Supported production channels</p><div className="post">Facebook Page <span className="tag">API</span></div><div className="post">Instagram Professional <span className="tag">API</span></div><div className="post">WhatsApp Business <span className="tag">API</span></div><div className="post">Website / Blog <span className="tag">API</span></div><p className="muted">WhatsApp Status and ordinary groups require a supported/manual workflow.</p></aside></section></main>}